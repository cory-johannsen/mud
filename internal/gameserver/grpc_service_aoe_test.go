package gameserver

import (
	"testing"
)

// TestHandleUse_AoE_SkipUntilFeatDefined is a placeholder: AoE integration tests
// require a feat/tech with aoe_radius > 0 in the YAML registry.
// The unit logic (Chebyshev radius filter) is exercised by the AoE radius helper tests.
func TestHandleUse_AoE_SkipUntilFeatDefined(t *testing.T) {
	t.Skip("AoE feat integration test: enable once a feat with aoe_radius > 0 is defined in YAML")
}
